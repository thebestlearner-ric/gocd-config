format_version: 10
pipelines:
  air-backend:
    group: air
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      air-backend:
        git: https://github.com/thebestlearner-ric/air-backend.git
        username: thebestlearner-ric
        shallow_clone: true
        auto_update: true
        branch: main
        encrypted_password: AES:PHp/CacxfxowpJmo7K92xw==:GWG53xoLL5a4svzdBrjqpw==
        destination: air-backend-repo
      air-backend-conf:
        git: https://github.com/thebestlearner-ric/gocd-config.git
        username: thebestlearner-ric
        shallow_clone: true
        auto_update: true
        branch: backend
        encrypted_password: AES:PHp/CacxfxowpJmo7K92xw==:GWG53xoLL5a4svzdBrjqpw==
        destination: air-backend-repo-config
    stages:
    - Build:
        fetch_materials: true
        keep_artifacts: false
        clean_workspace: false
        approval:
          type: success
          allow_only_on_success: false
        jobs:
          dockerbuild:
            environment_variables:
              DOCKER_USER: learningric
              DOCKER_REPO: learningric/air_artifact
            secure_variables:
              DOCKER_PASSWORD: AES:nrcrbKOVkOmW47i5nbCrLw==:RCiJPrwaCgKoEl9IP5sqGg==
            timeout: 0
            elastic_profile_id: demo-app
            tasks:
            - exec:
                command: bash 
                arguments:
                  - -c
                  - |
                    cd air-backend-repo
                    DOCKER_REPO=learningric/air_artifact
                    TAG_NAME=$(git rev-parse --short=8 HEAD)
                    docker build -t $DOCKER_REPO:backend-$TAG_NAME .
                    docker images
          dockerpush:
            timeout: 0
            elastic_profile_id: demo-app
            tasks:
            - exec:
                command: docker
                arguments:
                  - login 
                  - -u 
                  - "#{DOCKER_USER}" 
                  - -p
                  - "#{secureEnv('DOCKER_PASSWORD')}"
              environment_variables:
                DOCKER_USER: learningric
                DOCKER_REPO: learningric/air_artifact
                PASSWORD: "#{secureEnv('DOCKER_PASSWORD')}"              
            - exec:
                command: bash
                arguments:
                  - -c
                  - |
                    TAG_NAME=$(git rev-parse --short=8 HEAD)
                    docker push DOCKER_REPO:backend-TAG_NAME
                  

